# Настройка Facebook Scraper

Это руководство поможет вам настроить интеграцию с Facebook для получения данных с публичных страниц Facebook без использования официального API.

## ⚠️ Важные ограничения

**Facebook Scraper работает только с:**
- ✅ Публичными страницами (Pages)
- ✅ Публичными постами
- ✅ Публичными комментариями

**НЕ работает с:**
- ❌ Личными профилями пользователей
- ❌ Закрытыми/приватными страницами
- ❌ Постами с ограниченной видимостью

**Важно:** Использование скрейпинга может нарушать Terms of Service Facebook. Используйте ответственно и в соответствии с политикой Facebook.

## Установка

### Шаг 1: Установка библиотеки

```bash
pip install facebook-scraper
```

Или добавьте в `requirements.txt`:
```
facebook-scraper>=0.2.60
```

### Шаг 2: Базовое использование

Библиотека работает без дополнительной настройки для публичных страниц:

```python
from facebook_scraper import get_posts

for post in get_posts('premierbankso', pages=1):
    print(post['text'])
    print(post['likes'])
    print(post['comments'])
```

## Опциональная настройка (для обхода ограничений)

Если вы столкнетесь с ограничениями (капча, блокировки), можно использовать cookies:

### Получение cookies из браузера

1. Войдите в Facebook в браузере
2. Откройте Developer Tools (F12)
3. Перейдите в Application/Storage → Cookies → https://www.facebook.com
4. Скопируйте значение cookie `c_user` и `xs`
5. Сохраните в файл `cookies.txt` в формате:
   ```
   c_user=YOUR_C_USER_VALUE; xs=YOUR_XS_VALUE
   ```

### Использование cookies

```python
from facebook_client import FacebookScraperClient

client = FacebookScraperClient(cookies="cookies.txt")
data = await client.get_page_post_data("premierbankso")
```

## Использование API

### Пример запроса через curl

```bash
# Получить данные последнего поста страницы premierbankso
curl "http://localhost:8000/facebook/page/premierbankso"
```

### Пример запроса через Python

```python
import httpx

async def get_facebook_data():
    async with httpx.AsyncClient() as client:
        response = await client.get(
            "http://localhost:8000/facebook/page/premierbankso"
        )
        data = response.json()
        print(data)
```

### Пример POST запроса

```bash
curl -X POST "http://localhost:8000/facebook/page" \
  -H "Content-Type: application/json" \
  -d '{
    "page_username": "premierbankso"
  }'
```

## Структура ответа API

```json
{
  "success": true,
  "data": {
    "page_info": {
      "username": "premierbankso",
      "name": "Premier Bank",
      "about": "Описание страницы",
      "fan_count": 10000,
      "followers": 5000
    },
    "post": {
      "post_id": "123456789",
      "text": "Текст поста...",
      "time": "2024-01-15T10:00:00+00:00",
      "timestamp": 1705315200,
      "post_url": "https://facebook.com/...",
      "likes": 100,
      "comments": 25,
      "shares": 5,
      "reactions": {
        "👍": 80,
        "❤️": 15,
        "😮": 3,
        "😄": 2
      },
      "images": ["url1", "url2"],
      "video": null
    },
    "reactions": {
      "reactions_by_type": {
        "LIKE": 80,
        "LOVE": 15,
        "WOW": 3,
        "HAHA": 2,
        "SORRY": 0,
        "ANGER": 0
      },
      "total_reactions": 100,
      "raw_reactions": {
        "👍": 80,
        "❤️": 15,
        "😮": 3,
        "😄": 2
      }
    },
    "comments": {
      "comments": [
        {
          "comment_id": "comment_123",
          "text": "Текст комментария",
          "author": "Имя пользователя",
          "author_id": "user_id",
          "time": "2024-01-15T11:00:00+00:00",
          "likes": 3,
          "replies": []
        }
      ],
      "total_count": 25
    },
    "fetched_at": "2024-01-15T12:00:00"
  }
}
```

## Доступные эндпоинты

### GET `/facebook/page/{page_username}`
Получить полные данные последнего поста страницы.

**Параметры:**
- `page_username` (path) - Username страницы (например, `premierbankso`)

**Пример:**
```
GET /facebook/page/premierbankso
```

### POST `/facebook/page`
Получить данные последнего поста (POST метод).

**Body:**
```json
{
  "page_username": "premierbankso"
}
```

### GET `/facebook/page/{page_username}/info`
Получить только базовую информацию о странице.

**Пример:**
```
GET /facebook/page/premierbankso/info
```

## Обработка ошибок

### Ошибка: Страница не найдена
```json
{
  "error": "Не удалось получить посты со страницы premierbankso: ...",
  "status_code": 400
}
```

**Решение:** 
- Проверьте правильность username страницы
- Убедитесь, что страница публичная
- Попробуйте использовать cookies (см. выше)

### Ошибка: Ограничения Facebook
Если вы получаете ошибки связанные с блокировкой или капчей:

1. Используйте cookies из браузера
2. Добавьте задержки между запросами
3. Используйте прокси (требует модификации кода)

## Лимиты и ограничения

Facebook активно борется с автоматизированным сбором данных:

- **Rate Limits:** Facebook может блокировать IP при частых запросах
- **Капча:** Может появляться при подозрительной активности
- **Изменения структуры:** Facebook может изменить HTML структуру, что сломает скрейпер

**Рекомендации:**
1. Используйте cookies для более стабильной работы
2. Добавьте задержки между запросами
3. Мониторьте изменения в библиотеке facebook-scraper
4. Обновляйте библиотеку регулярно: `pip install --upgrade facebook-scraper`

## Troubleshooting

### Проблема: "ModuleNotFoundError: No module named 'facebook_scraper'"

**Решение:**
```bash
pip install facebook-scraper
```

### Проблема: "Не удалось получить посты"

**Возможные причины:**
1. Страница не публичная
2. Неправильный username
3. Facebook заблокировал запросы

**Решение:**
1. Проверьте доступность страницы в браузере
2. Используйте cookies
3. Попробуйте позже

### Проблема: "Комментарии не загружаются"

**Решение:**
- Убедитесь, что комментарии публичные
- Некоторые посты могут не иметь комментариев
- Попробуйте использовать cookies

## Пример полного workflow

```python
import asyncio
from facebook_client import FacebookScraperClient

async def main():
    # Инициализация клиента
    # Опционально: client = FacebookScraperClient(cookies="cookies.txt")
    client = FacebookScraperClient()
    
    # Получение данных
    data = await client.get_page_post_data("premierbankso")
    
    # Обработка данных
    post = data["post"]
    reactions = data["reactions"]
    comments = data["comments"]
    
    print(f"Последний пост: {post['text']}")
    print(f"Всего реакций: {reactions['total_reactions']}")
    print(f"Комментариев: {comments['total_count']}")
    
    # Вывод реакций по типам
    for reaction_type, count in reactions['reactions_by_type'].items():
        if count > 0:
            print(f"{reaction_type}: {count}")

if __name__ == "__main__":
    asyncio.run(main())
```

## Полезные ссылки

- [facebook-scraper GitHub](https://github.com/kevinzg/facebook-scraper)
- [facebook-scraper PyPI](https://pypi.org/project/facebook-scraper/)
- [Facebook Terms of Service](https://www.facebook.com/terms)

## Важные замечания

⚠️ **Юридические аспекты:**
- Использование скрейпинга может нарушать Terms of Service Facebook
- Используйте только для публичных данных
- Не собирайте персональные данные без согласия
- Соблюдайте законы о защите данных (GDPR, CCPA и т.д.)

⚠️ **Технические аспекты:**
- Facebook может изменить структуру сайта в любой момент
- Библиотека может перестать работать после обновлений Facebook
- Рекомендуется иметь резервный план (официальный API, если возможно)
